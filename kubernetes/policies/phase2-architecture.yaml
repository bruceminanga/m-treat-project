apiVersion: kyverno.io/v1
kind: ClusterPolicy
metadata:
  name: enforce-pod-architecture
spec:
  validationFailureAction: Enforce
  rules:
    - name: require-image-digest
      match:
        any:
        - resources:
            kinds: [Pod]
      exclude:
        any:
        - resources:
            namespaces: [kube-system, kyverno, kube-node-lease, kube-public]
      validate:
        message: "Images must be pinned with immutable SHA256 digest (repo/app@sha256:...)."
        pattern:
          spec:
            containers:
              - image: "*@sha256:*"

    - name: require-spread-constraints
      match:
        any:
        - resources:
            kinds: [Deployment, StatefulSet]
      exclude:
        any:
        - resources:
            namespaces: [kube-system, kyverno, kube-node-lease, kube-public]
      validate:
        message: "Deployments and StatefulSets must declare topologySpreadConstraints across zones or hosts."
        pattern:
          spec:
            template:
              spec:
                topologySpreadConstraints:
                  - maxSkew: ">0"
                    topologyKey: "?*"
                    whenUnsatisfiable: "DoNotSchedule | ScheduleAnyway"
